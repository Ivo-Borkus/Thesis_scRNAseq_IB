print(setting)

if (setting == "Response") {

} else if (setting == "Primary") {


} else if (setting == "Sample") {
    # selected
} else {
    print("setting selected is not found, using different datasets as groups")
}
